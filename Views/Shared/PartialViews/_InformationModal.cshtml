<div class="modal fade un-select" tabindex="-1" role="dialog" aria-labelledby="mySmallModalLabel" aria-hidden="true" id="information-modal">
    <div class="modal-dialog modal-lg">
        <div class="modal-content text-center">
            <div class="modal-header direction font-cairo">
                <button type="button" class="close-modal float-direction-reserve" data-dismiss="modal" aria-label="Close" style="float: left"><span aria-hidden="true">&times;</span></button>
                <div class="matterix-color display-4 title-1-5" id="information-modal-title">{{$t('message.confirm')}}</div>
            </div>
            <div id="information-modal-content" class="text-center direction p-3"></div>
            <div class="modal-footer direction text-center">
                <button type="button" class="m-btn m-btn-small m-btn-primary text-center" id="information-modal-close">{{$t('message.close')}}</button>
                @* <button type="button" class="m-btn m-danger-btn text-center" id="information-modal-close1">{{$t('message.close')}}</button> *@
            </div>
        </div>
    </div>
</div>